# Environment
.env

# Binary (only the compiled binary at root, not the cmd/server/ source directory)
/server
/server.exe
*.exe

# Database
data/
*.db
*.db-shm
*.db-wal

# Uploads (user content, not source)
web/static/uploads/*
!web/static/uploads/.gitkeep

# Go
*.test
*.out
vendor/

# OS
.DS_Store
Thumbs.db
